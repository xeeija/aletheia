mutation CreateRandomWheel($name: String) {
  createRandomWheel(name: $name) {
  	... on RandomWheel {
			...RandomWheelDetails
 	 }
  	# ... on AppError {
    #   errorMessage
    #   errorCode
  	# 	fieldErrors {
    # 		field
    # 		message
  	# 	}
		# }
  }
}