mutation CreateRandomWheel($name: String) {
  createRandomWheel(name: $name) {
  	... on RandomWheel {
    	id
      slug
      name
      createdAt
      ownerId
 	 }
  	... on AppError {
      errorMessage
      errorCode
  		fieldErrors {
    		field
    		message
  		}
		}
  }
}