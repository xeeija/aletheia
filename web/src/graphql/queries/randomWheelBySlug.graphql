query RandomWheelBySlug($slug: String!) {
  randomWheelBySlug(slug: $slug) {
    ...RandomWheelDetails
    entries {
      ...RandomWheelEntry
    }
    winners {
      ...RandomWheelWinner
    }
    owner {
      ...UserName
    }
    members {
      userId
      roleName
    }
  }
}
