import { PageContainer } from "@/app/components"
import { Layout } from "@/types"

const RandomwheelLayout: Layout = ({ children }) => {
  return <PageContainer title="Random Wheel">{children}</PageContainer>
}

export default RandomwheelLayout
